/**
 * Public application API for M18. Controllers and other modules may depend only on this named
 * interface — never on M18 repositories, JPA entities, provider adapters, or internal handlers.
 */
@org.springframework.modulith.NamedInterface("api")
package com.orderpilot.aibot.api;
